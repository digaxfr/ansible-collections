# Ansible managed
[Match]
Name=en*

[Network]
DHCP=no
{% for macvlan in macvlans %}
MACVLAN={{ macvlan.name }}
{% endfor %}
{% for macvtap in macvtaps %}
MACVTAP={{ macvtap.name }}
{% endfor %}

[IPv6AcceptRA]
Token=eui64
Token=prefixstable
